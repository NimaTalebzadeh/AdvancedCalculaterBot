services:
  bot:
    build: .
    container_name: advanced-calculator-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOTTOKEN=${TELEGRAM_BOTTOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
      - PORT=5000
    ports:
      - "5000:5000"
